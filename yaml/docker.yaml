# a docker yaml template
- scope: process
  name: "template"
  container: "ubuntu:24.04"
  environment: "docker"
  parallel: False
  var:
    extra_args: ""
    my_demo_argument: "This is just a demo."
    mk.output: "./test/test-output"

# - scope: global
#   var:
#     mk.output: "./test/test-output"